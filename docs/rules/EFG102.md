# EFG102 — Required column may reject existing rows

A non-null column is added without a detectable default or staged backfill. Add it compatibly, backfill in bounded batches, deploy code that requires it, and enforce the final constraint later.
