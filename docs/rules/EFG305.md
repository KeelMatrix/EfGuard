# EFG305 — Transaction semantics require review

An operation suppresses its transaction. This finding is independent of SQL semantic classification, so arbitrary SQL can also retain EFG399 and an unbounded backfill can also retain EFG304. Isolate the operation from changes that need atomicity and verify provider/deployment behavior.
