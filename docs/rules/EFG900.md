# EFG900 — Unsupported database provider

EfGuard v1 supports SQL Server/Azure SQL and Npgsql PostgreSQL. It does not guess locking or compatibility behavior for other providers and exits with execution code `2`.
