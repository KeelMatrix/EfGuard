# EFG201 — Destructive change

The migration drops a column or table. This can lose data and break rollback. Preserve recoverability and perform the contract step only after dependent application generations are retired.
