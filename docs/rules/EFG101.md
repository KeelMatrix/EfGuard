# EFG101 — Rolling-deployment incompatibility

The previous application model still maps a table or column that the target migration drops or renames. Use expand/transition/contract: deploy code that no longer requires the old name, wait for old instances to retire, then contract the schema.
