# EFG301 — Potentially blocking SQL Server index creation

The SQL Server index is not marked online. When provider-generated migration SQL for the operation is available, it is retained in `providerSql` as evidence; runtime lock duration still depends on engine edition, version, index shape, workload, and deployment conditions. Without matching generated-SQL evidence, this finding is explicitly `UNVERIFIED`. Configure online creation where supported or schedule a maintenance window, and validate the actual engine behavior separately.
