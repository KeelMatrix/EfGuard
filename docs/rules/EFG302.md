# EFG302 — Write-blocking PostgreSQL index creation

The PostgreSQL index is not configured for concurrent creation. Use the Npgsql concurrent option when appropriate and honor its transaction requirements. Workload-dependent lock behavior remains uncertain.
