# Contributing

Build and test locally with the commands in `AGENTS.md`. Add focused regression coverage for every rule or provider behavior change. Keep reports, fixtures, and documentation free of credentials and machine-specific paths. Do not add a workflow that runs automatically in a private repository.
